# mermaid-swimming-app

Steps to Use:

* Clone the repository from the URL above

* Build the application by running command : mvn clean install

* Run the application by running command : mvn spring-boot:run

* Open a new browser and launch url: http://localhost:8080/login

* Login using credentials : member : password
